# Project-level Cargo registry config.
# Overrides a possibly broken machine-global USTC mirror so new Rust deps can download.
[source.crates-io]
replace-with = "rsproxy"

[source.rsproxy]
registry = "sparse+https://rsproxy.cn/index/"
