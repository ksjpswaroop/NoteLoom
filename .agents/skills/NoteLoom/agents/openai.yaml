interface:
  display_name: "NoteLoom"
  short_description: "Repo-specific patterns and workflows for NoteLoom"
  default_prompt: "Use the NoteLoom repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true